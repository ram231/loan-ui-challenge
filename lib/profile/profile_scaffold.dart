import 'package:flutter/material.dart';

class HiRamProfileBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(
          color: Colors.grey,
          height: MediaQuery.of(context).size.height / 2,
        ),
      ],
    );
  }
}
